BUILDDIR_MASTER=build_master
BUILDDIR_MASTER_2=build_master2
BUILDDIR_SLAVE_1=build_slave1
BUILDDIR_SLAVE_2=build_slave2
BUILDDIR_SLAVE_3=build_slave3
BUILDDIR_SLAVE_4=build_slave4
BUILDDIR_SLAVE_5=build_slave5

# Target chips
TARGET_MASTER=esp32s3
TARGET_MASTER_2=esp32s3
TARGET_SLAVE_1=esp32s3
TARGET_SLAVE_2=esp32s3
TARGET_SLAVE_3=esp32s3
TARGET_SLAVE_4=esp32c3
TARGET_SLAVE_5=esp32s3

# include config.local file in the same directory
_config_dir="$(cd "$(dirname "${BASH_SOURCE[0]:-$0}")" && pwd)"
source "$_config_dir/config.local"